apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: venu-market-agent
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: venu-market-agent
spec:
  project: default
  sources:
    - repoURL: https://github.com/uzvenu/deploy.git
      targetRevision: master
      path: charts/agents
      helm:
        releaseName: venu-market-agent
        valueFiles:
          - $values/helm/venu-market-values.yaml
    - repoURL: https://github.com/uzvenu/chatbot.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: venu-market-agent
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
    retry:
      limit: 5
      backoff:
        duration: 10s
        maxDuration: 3m
        factor: 2
